name: Benchmarks

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  bench-linux-x86_64:
    name: bench (linux, x86_64)
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      FORCE_COLOR: "1"
      PYTEST_ADDOPTS: "--color=yes"
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      # Same reason as the Tests workflow: a free Linux runner's 16 GB is not
      # quite enough for the heaviest unit, and /mnt has ~70 GB going spare.
      - name: More swap
        run: |
          sudo fallocate -l 24G /mnt/swapfile
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          free -h

      - uses: prefix-dev/setup-pixi@v0.10.2
        with:
          frozen: true
          cache: true
          cache-write: ${{ github.event_name == 'push' && github.ref_name == 'main' }}

      - name: Checkout gh-pages
        if: github.ref == 'refs/heads/main'
        uses: actions/checkout@v7
        with:
          ref: gh-pages
          path: gh-pages-out

      # One selection is one compilation unit, and `bench_*.mojo` builds at -O3:
      # all 283 benchmarks in a single `pytest` call was OOM-killed on the free
      # runner 15 minutes into the compile, on every run since 2026-05-11.  The
      # selections below are separate units, so each one fits.  Their results
      # are merged rather than overwritten -- see `BenchmarkHistory`.
      #
      # **One file per invocation**, and grouping them is worse rather than
      # better. Five of the kernel bench files as a single unit did not finish
      # the -O3 compile in 3,600 s; the same five as five units compile in 165 s
      # in total (20 + 10 + 86 + 12 + 37, measured on Linux 2026-09-08). The
      # combined unit blows up superlinearly, so paying for five elaborations is
      # the cheap option here -- the opposite of the tests, where one directory
      # per unit is what fits.
      #
      # `--benchmark` *adds* the bench files, it does not replace the tests, so
      # naming a directory here compiles and runs that directory's test unit as
      # well -- which is what the Tests workflow is for, and which is how this
      # job came to be re-running `marrow/expr`, the selection that cannot fit a
      # free runner. Naming `bench_*` files leaves the test unit empty, so this
      # job benchmarks and nothing else.
      - name: Run benchmarks (PR)
        if: github.ref != 'refs/heads/main'
        run: |
          for f in $(find marrow -name 'bench_*.mojo' | sort); do
            pixi run -e bench pytest --benchmark -v --no-gpu --mojo-timeout 1800 "$f"
          done
          pixi run -e bench pytest --benchmark -v --no-gpu python/marrow/tests/bench_*.py

      - name: Run benchmarks
        if: github.ref == 'refs/heads/main'
        run: |
          save() {
            pixi run -e bench pytest --benchmark -v --no-gpu --mojo-timeout 1800 \
              --save-benchmarks results \
              --benchmark-history gh-pages-out/benchmarks/data.json "$@"
          }
          for f in $(find marrow -name 'bench_*.mojo' | sort); do
            save "$f"
          done
          save python/marrow/tests/bench_*.py

      - name: Prepare dashboard
        if: github.ref == 'refs/heads/main'
        run: |
          COMMIT="$(git rev-parse HEAD)"
          mkdir -p gh-pages-out/results gh-pages-out/benchmarks
          cp results/${COMMIT}.json gh-pages-out/results/${COMMIT}.json
          cp benchmarks/index.html gh-pages-out/benchmarks/index.html

      - uses: peaceiris/actions-gh-pages@v4
        if: github.ref == 'refs/heads/main'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./gh-pages-out
          keep_files: true
