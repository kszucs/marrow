name: Docs

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  docs-build:
    name: docs (build)
    runs-on: ubuntu-latest
    env:
      MODULAR_HOME: "/home/runner/.modular"
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0
    - uses: prefix-dev/setup-pixi@v0.9.4
      with:
        frozen: true
        cache: true
        cache-write: ${{ github.event_name == 'push' && github.ref_name == 'main' }}
        environments: docs
    - name: Detect CPU feature level
      id: cpu
      run: |
        if grep -q avx512f /proc/cpuinfo; then echo "level=v4" >> $GITHUB_OUTPUT
        elif grep -q avx2 /proc/cpuinfo; then echo "level=v3" >> $GITHUB_OUTPUT
        elif grep -q avx /proc/cpuinfo; then echo "level=v2" >> $GITHUB_OUTPUT
        else echo "level=v1" >> $GITHUB_OUTPUT; fi
    - uses: actions/cache@v5
      with:
        path: .pixi/envs/docs/**/.mojo_cache
        key: mojo-cache-docs-${{ runner.os }}-${{ steps.cpu.outputs.level }}-${{ hashFiles('pixi.lock', '**/*.mojo') }}
        restore-keys: mojo-cache-docs-${{ runner.os }}-${{ steps.cpu.outputs.level }}-

    - name: Build docs
      run: pixi run -e docs docs

    - uses: peaceiris/actions-gh-pages@v4
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./docs/_site
        keep_files: true
