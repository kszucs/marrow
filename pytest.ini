[pytest]
# Tests and benchmarks live next to the code they cover (marrow/**/tests/,
# python/marrow/tests/), so the roots are just the two source trees.
testpaths = marrow python
pythonpath = python
python_files = test_*.py bench_*.py
addopts =
