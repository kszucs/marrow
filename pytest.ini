[pytest]
# Tests and benchmarks live next to the code they cover (marrow/**/tests/,
# python/marrow/tests/); `golden/` is the cross-lane query corpus.
testpaths = marrow python golden
pythonpath = python
python_files = test_*.py bench_*.py
addopts =
