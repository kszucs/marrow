[pytest]
testpaths = marrow python
pythonpath = python
python_files = test_*.py bench_*.py
# NOTE: --pkg (precompiled-marrow fast build) is NOT enabled by default: the
# Mojo compiler currently crashes consuming the non-elaborated package for some
# generic/compute-heavy files (filter, join, plan, pushdown, streaming,
# hashtable).  Opt in per-invocation with --pkg when iterating on unaffected
# modules; the full suite must run from source.
addopts =
