Licences used:
{{#each overview}}
  {{{name}}} ({{id}}): {{count}} crates
{{/each}}

{{#each licenses}}
================================================================================
{{{name}}} ({{id}})

Used by:
{{#each used_by}}
  {{crate.name}} {{crate.version}}
{{/each}}

{{{text}}}
{{/each}}
